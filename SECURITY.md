# 安全说明

## 支持范围

当前仅维护最新的 V1.9.x 版本。安全修复会优先进入最新版本，不保证为早期测试 APK 单独发布补丁。

## 报告安全问题

请使用 GitHub 仓库的 **Security → Report a vulnerability** 私下提交安全问题。不要通过公开 Issue 披露可利用细节。

报告中建议包含：

- 受影响版本和 Android 版本；
- 复现步骤和可能影响；
- 最小化、脱敏后的示例；
- 你认为可行的修复方向（可选）。

请勿提交真实备份、健康信息、位置、密码、APK 签名密钥或其他个人数据。

## 重要安全边界

- LifeHub 默认把数据保存在应用私有目录。
- 应用关闭 Android 系统备份，避免数据库被系统自动复制到云端。
- 加密备份密码不会上传到服务器；密码丢失后无法恢复加密内容。
- 发布签名密钥不属于本仓库内容，任何贡献者都不应提交密钥或口令。
